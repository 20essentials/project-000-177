<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title or "Curl" }}</title>
  <meta name="google" content="notranslate" />
  <link rel="shortcut icon" href="{{ favicon or "assets/favicon.webp" }}" type="image/x-icon" />
  <style>
  *,
  *::after,
  *::before {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue';
    -webkit-tap-highlight-color: #fff8;
  }

  html {
    scroll-behavior: smooth;
    scrollbar-width: thin;
    scrollbar-color: white #fff8;
  }

  body {
    height: 100dvh;
    display: flex;
    flex-wrap: wrap;
    place-content: center;
  }

  .scheme {
    width: 100%;
    height: 100dvh;
    inset: 0;
    position: fixed;
    background-image: linear-gradient(to right,
        #0008 0 50%,
        #fff8 50% 100%);
    background-size: 100px 100px;
    position: relative;
    background-blend-mode: multiply;
  }
  
  .scheme::before {
    content: '';
    display: block;
    width: 100%;
    height: 100%;
    position: fixed;
    inset: 0;
    z-index: -1;
    background-size: 100px 100px;
    background-image: linear-gradient(to top,
        #0008 0 50%,
        #fff8 50% 100%);
    animation: mover 5s linear infinite alternate both;
  }

  @media (width <= 1000px) {
    .scheme::before {
      animation: moverMobile 5s linear infinite alternate both;
    }
  }

  @keyframes mover {
    0% {
      background-size: 100px 100px;
    }

    100% {
      background-size: 500px 1000px;
    }
  }

  @keyframes moverMobile {
    0% {
      background-size: 20px 20px;
    }

    100% {
      background-size: 100px 100px;
    }
  }
  </style>
</head>
<body>
  <section class="scheme"></section>
</body>
</html>
